# قائمة مراجعة الجاهزية للإنتاج (Production Checklist)

لضمان عمل المشروع بشكل سليم وواقعي في بيئة حقيقية، يرجى مراجعة النقاط التالية التي تم تنفيذها وما تبقى عليك القيام به:

## ✅ ما تم تنفيذه برمجياً
- [x] **هيكل المشروع**: تقسيم نظيف (Controllers, Services, Routes) لسهولة الصيانة.
- [x] **الأمان**: إضافة `Basic Auth` للاتصال بميسر و `Secret Token` للاتصال بـ Ecwid.
- [x] **معالجة الأخطاء**: نظام موحد يضمن عدم توقف السيرفر عند حدوث خطأ في الطلب.
- [x] **التسجيل (Logging)**: نظام يسجل العمليات (طلبات ناجحة/فاشلة) لمتابعة ما يحدث.
- [x] **المزامنة التلقائية**: Webhook handler يحول حالة الطلب في Ecwid تلقائياً عند الدفع.

## 🛠 ما يجب عليك فعله للتشغيل "الواقعي"

### 1. إعدادات الحسابات (حرج جداً)
- [ ] **تبديل المفاتيح**: تأكد من استخدام `MOYASAR_API_KEY_LIVE` في البيئة الحقيقية وليس مفتاح الاختبار.
- [ ] **صلاحيات Ecwid**: تأكد أن الـ Token الخاص بـ Ecwid لديه صلاحية `edit_orders` لتغيير حالة الطلب إلى PAID.

### 2. واجهة المستخدم (UI/UX)
- [ ] **صفحة الـ Checkout**: الملف `routes/checkout.js` يحتوي حالياً على كود HTML بسيط جداً. يفضل استبداله بتصميم احترافي يتماشى مع هوية متجرك.
- [ ] **صفحات النجاح والفشل**: يجب تصميم صفحات CSS جذابة تخبر العميل بنجاح العملية أو فشلها مع زر "العودة للمتجر".

### 3. الاستضافة (Hosting)
- [ ] **الدومين (Domain)**: يفضل ربط تطبيق Vercel بدومين خاص أو Subdomain (مثلاً `payments.yourstore.com`).
- [ ] **HTTPS**: تأكد أن جميع الروابط تعمل عبر `https` وهو ما توفره Vercel تلقائياً.

### 4. الاختبارات النهائية (Final Tests)
- [ ] **اختبار ميسر التجريبي**: قم بعمل طلب حقيقي باستخدام "بطاقة اختبار" من ميسر للتأكد من أن الـ Webhook يصل للسيرفر ويحدث الطلب في Ecwid.
- [ ] **اختبار العملة**: تأكد أن العملة في Ecwid هي `SAR` لتتطابق مع إعدادات ميسر.

## ⚠️ نقاط تقنية للنقاش
- **قاعدة البيانات**: حالياً المشروع لا يستخدم قاعدة بيانات (Stateless). هذا جيد للبساطة، لكن لو رغبت في الاحتفاظ بسجل للعمليات (Logs) لفترة طويلة، قد نحتاج لربط `MongoDB` أو `PostgreSQL`.
- **التحقق المزدوج**: هل ترغب في إضافة خطوة "ID verification" إضافية للطلبات الكبيرة؟

---
**الخلاصة**: الكود الحالي جاهز تماماً للعمل، وما ينقصه فقط هو "التجميل" (UI) ووضع المفاتيح الحقيقية.
